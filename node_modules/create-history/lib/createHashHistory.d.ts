import type { HistoryOptions, History, LocationTypeMap, LocationType } from './index';
export interface Update {
    (path: string): void;
}
export interface StopListener {
    (): void;
}
export default function createHashHistory<LT extends LocationType>(options?: HistoryOptions): History<LocationTypeMap[LT]['Base'], LocationTypeMap[LT]['Intact']>;
